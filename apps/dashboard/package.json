{
  "name": "@medical-ai-qa/dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "node ./scripts/clean-dashboard.mjs",
    "dev": "pnpm run clean && next dev --port 3001",
    "build": "next build",
    "rebuild": "pnpm run clean && next build",
    "start": "next start --port 3001",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@medical-ai-qa/shared-types": "workspace:*",
    "next": "^15.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4"
  }
}
